dist: trusty
sudo: required

language:
    - c++

cache:
    - apt

before_install:
    - sudo bash scripts/ros_install.bash
    - sudo bash scripts/apriltag_install.bash
    - sudo bash scripts/husky_simulator_install.bash
    - sudo bash scripts/px4_simulator_install.bash

script:
    - echo "Done :)"

notifications:
    recipients:
        - chutsu@gmail.com
        - stan.j.brown@gmail.com
    email:
        on_success: change
        on_failure: always
