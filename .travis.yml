dist: trusty
sudo: required

language:
  - c++

cache:
  - apt

before_install:
  - bash scripts/ros_install.sh
  - bash scripts/hector_quad_simulator_install.sh

notifications:
    recipients:
        - stan.j.brown@gmail.com
        - chutsu@gmail.com
    email:
        on_success: change
        on_failure: always
